import React from 'react';
import { Link } from 'react-router';
import styles from './clients.less';

const Clients = React.createClass({
    render() {
        return (
            <div className="clients">
                <h1>
                    Client Setup
                </h1>
            </div>
        )
    }
});

module.exports = Clients;
